# Linux-specific install config

- link:
    ~/.xmodmap_original: configs/xmodmap_original
    ~/.xmodmap: configs/xmodmap
    ~/.xkbmap: configs/xkbmap

# Homebrew formulas

- brew:
  - fzf
  - hub
  - rbenv
